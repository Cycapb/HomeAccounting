@model IEnumerable<Product>
@if (Model.Count() == 0)
{
    @: Нет подкатегорий
}
else
{
    foreach (var item in Model)
    {
            <p>
                @string.Join(",", item.ProductName)
            </p>
    }
}
